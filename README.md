# InstantScrape
InstantScrape is an ASP.NET Core MVC app that scrapes real-time data based on Category and Place using Selenium. It fetches the latest 20 search results and displays them in a clean UI or returns them as JSON. Fast, simple, and easy to extend for custom scraping needs.
